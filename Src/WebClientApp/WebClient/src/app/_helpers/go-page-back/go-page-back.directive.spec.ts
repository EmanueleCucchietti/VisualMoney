import { GoPageBackDirective } from './go-page-back.directive';

describe('GoPageBackDirective', () => {
  it('should create an instance', () => {
    const directive = new GoPageBackDirective();
    expect(directive).toBeTruthy();
  });
});
